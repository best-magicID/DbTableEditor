<Window x:Class="DbTableEditor.Views.WindowAddOrChangeTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DbTableEditor.ViewModels"
        
        mc:Ignorable="d"
        Title="Окно добавления или редактирвоания таблицы" 
        Height="300" 
        Width="300" 
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        
        <Style TargetType="TextBlock">
            <Setter Property="Margin" 
                    Value="2.5"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Margin" 
                    Value=" 0 2.5"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Margin" 
                    Value=" 0 2.5"/>
        </Style>
        
    </Window.Resources>

    <Grid>
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Имя таблицы"
                       Grid.Row="0"/>

            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Text="{Binding NameTable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <DockPanel Grid.Row="1"
                       Grid.Column="0"
                       Grid.ColumnSpan="2">
                <DataGrid/>
            </DockPanel>

            <Button Content="Сохранить"
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="0 2.5 0 0"
                    HorizontalAlignment="Right"/>

        </Grid>
    </Grid>
</Window>
